Screenshots captured from the running demo. Replace with actual captures after `docker compose up --build`.

| Screenshot | Description |
|------------|-------------|
| login.png | Login page with demo accounts |
| dashboard.png | Regulatory dashboard with status KPIs |
| applications.png | Application list with filters |
| detail.png | Application detail with checklist and workflow |
| audit.png | Audit log view (admin/auditor) |

To capture:
1. `docker compose up --build`
2. Open http://localhost:5180 (Docker) or http://localhost:5173 (local dev)
3. Login and navigate each page
